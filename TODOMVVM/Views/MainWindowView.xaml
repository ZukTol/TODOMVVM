<Window x:Class="TODOMVVM.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Style="{DynamicResource MainWindowStyle}"
        Width="500" SizeToContent="Height">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/MainResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <StackPanel>
        <TextBlock Text="todos" Style="{StaticResource HeaderTextBlockStyle}"/>
        <TextBox Text="{Binding NewToDoText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AddNewToDoTextBoxStyle}">
            
        </TextBox>
    </StackPanel>
</Window>
